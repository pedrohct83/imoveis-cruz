!! dashboard.js join faz com q a mensagem Filtrando por apareça sempre

x) tirar o hide do index-sidebar.ejs#collapseFilters só no desktop?
R: if (document.documentElement.clientWidth < 992) {add hide class to #collapseFilters}

1) Inquilinos
- tenant type choice will show/hide cpf/cnpj (melhor criar dois index diferentes no tenant schema, um pra cpf outro pra cnpj e lidar com a logica no router)
- input masks
- Editar inquilino

2) ordem alfabética considerar números

4) criar uma função única pros togglers no main.js (fazer primeiro da tenant e sale (que nem existe mais, mas blz))